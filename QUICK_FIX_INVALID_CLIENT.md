# ⚡ 快速修復 "invalid_client" - 3 步驟

## 🎯 最可能的原因

Supabase 中的 **Client ID** 或 **Client Secret** 與 Google Cloud Console 不匹配。

---

## ✅ 3 步驟修正法

### 步驟 1: 重新複製並設定 Client ID（最重要）

1. **前往 Google Cloud Console**:
   - APIs & Services → Credentials
   - 點擊 OAuth 2.0 Client ID: `megan-oauth-new0`
   - **複製完整的 Client ID**:
     ```
     817238464028-qot5sc882lgp90f8fq5fhk85bni11q27.apps.googleusercontent.com
     ```

2. **前往 Supabase Dashboard**:
   - Authentication → Providers → Google
   - **清空** Client IDs 欄位
   - **重新貼上**剛才複製的 Client ID
   - ⚠️ **檢查**：
     - 沒有多餘空格
     - 沒有多餘的字元
     - 完全一致

3. **儲存**：
   - 點擊 **Save** 按鈕
   - 等待 5 秒

---

### 步驟 2: 重新設定 Client Secret

1. **前往 Google Cloud Console**:
   - 在同一個 OAuth 2.0 Client ID 頁面
   - 找到 **Client Secret**
   - 如果看不到，點擊「重設密鑰」重新建立
   - **複製完整的 Client Secret**:
     ```
     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
     ```

2. **前往 Supabase Dashboard**:
   - Authentication → Providers → Google
   - **清空** Client Secret 欄位
   - **重新貼上**剛才複製的 Client Secret
   - ⚠️ **檢查**：
     - 沒有多餘空格
     - 包含所有連字號和底線

3. **儲存**：
   - 點擊 **Save** 按鈕
   - 等待 5 秒

---

### 步驟 3: 確認並等待

1. **確認設定**：
   - ✅ Enable Sign in with Google: **ON**
   - ✅ Client IDs: 已正確填入
   - ✅ Client Secret: 已正確填入

2. **等待**：
   - 等待 **5-10 分鐘**（Google 設定需要時間生效）

3. **清除快取並測試**：
   - 清除瀏覽器快取和 Cookies
   - 訪問 `http://localhost:3000/login`
   - 點擊「使用 Google 登入」

---

## 🔍 關鍵檢查點

### ⚠️ Client ID 必須完全一致

```
Google Cloud Console:
817238464028-qot5sc882lgp90f8fq5fhk85bni11q27.apps.googleusercontent.com

Supabase 中必須是:
817238464028-qot5sc882lgp90f8fq5fhk85bni11q27.apps.googleusercontent.com
```

**常見錯誤**：
- ❌ 複製時遺漏部分字元
- ❌ 有多餘的空格
- ❌ 手動輸入時打錯字

**建議**：
- ✅ 使用複製/貼上，不要手動輸入
- ✅ 貼上後逐字檢查

---

## 🚨 如果仍然失敗

如果完成以上 3 步驟後仍然出現錯誤：

1. **截圖 Supabase 設定頁面**（遮罩敏感資訊）
   - 顯示 Client IDs 欄位的完整內容
   - 確認 Callback URL 顯示正確

2. **檢查瀏覽器 Console**（F12 → Console）
   - 查看是否有其他錯誤訊息

3. **檢查 Network 請求**（F12 → Network）
   - 點擊登入按鈕
   - 查看跳轉的 Google OAuth URL
   - 檢查 URL 中的 `client_id` 參數

---

## 📝 快速檢查清單

在開始之前，確認：

- [ ] 已登入 Supabase Dashboard
- [ ] 已登入 Google Cloud Console
- [ ] 有權限修改 OAuth 設定
- [ ] 瀏覽器可以正常訪問 Google 服務

---

**提示**：最常見的錯誤是 Client ID 複製不完整或有多餘空格。請特別注意這點！







